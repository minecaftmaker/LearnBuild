# Scale target

The repository deliberately uses versioned content files rather than putting the entire curriculum into one opaque database blob. This makes lessons, challenges and projects easy to review, fork and extend.
