# Content model

A lesson has: id, track, title, level, duration, XP, concept, prediction, build task, test, reflection and next step. A project adds milestones, rubric, starter files, tests and a README checklist.

The UI can consume generated Markdown/JSON content without changing the shell of the application.
