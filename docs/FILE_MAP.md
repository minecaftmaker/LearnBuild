# Repository map

`src/data` is the curriculum brain. `src/pages` is the product. `src/components` is the reusable teaching UI. `labs` contains language projects. `supabase` holds database policy/schema. `docs` explains architecture and pedagogy.
