# Hosting Lab

The Hosting Lab is intentionally practical: static hosting, DNS, HTTPS, CI/CD, logs, health checks, containers and scaling. Each stage can be mapped to a real provider later.

The simulator is local-first; provider credentials belong in a server-side integration, not in GitHub Pages JavaScript.
