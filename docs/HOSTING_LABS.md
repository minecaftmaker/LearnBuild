# Hosting labs

The platform teaches hosting through safe disposable exercises: static deployment, DNS, HTTPS, environment configuration, logs, health checks, containers, reverse proxies and scaling.

Learners should first run services locally, then deploy the same artifact to a hosted environment. The lesson asks them to compare local and hosted behavior instead of treating deployment as a button.
