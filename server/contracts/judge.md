# Judge contract

A submission contains project id, language, source files and requested test suite. The runner returns compile status, bounded stdout/stderr, test results, timing and artifact metadata—not arbitrary host access.
