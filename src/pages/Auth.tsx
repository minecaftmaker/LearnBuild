import {useState} from 'react';import {signInWithEmail,signInWithProvider} from '../lib/auth';export function Auth(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [message,setMessage]=useState('');const start=async(p:'google'|'apple')=>{try{const r=await signInWithProvider(p);setMessage(`Provider ready: ${r.provider}. Configure Supabase OAuth to finish sign-in.`)}catch(e){setMessage((e as Error).message)}};return <div className="auth-card"><h2>Welcome to LearnBuild</h2><p>Save your progress across devices. Continue as a guest, or connect an account.</p><div className="auth-providers"><button onClick={()=>start('google')}>Continue with Google</button><button onClick={()=>start('apple')}>Continue with Apple</button></div><div className="divider">or email</div><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" type="email"/><input value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" type="password"/><button className="primary full" onClick={async()=>setMessage(JSON.stringify(await signInWithEmail(email,password)))}>Sign in / create account</button>{message&&<small>{message}</small>}<p className="muted tiny">For real accounts, set the VITE_SUPABASE_* variables and enable Google, Apple and email providers in Supabase.</p></div>}
