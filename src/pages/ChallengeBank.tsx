import {useMemo,useState} from 'react';import {challenges} from '../data/challenges';export function ChallengeBank(){const[q,setQ]=useState('');const[f,setF]=useState('All');const items=useMemo(()=>challenges.filter(c=>(f==='All'||c.track===f)&&`${c.title} ${c.track}`.toLowerCase().includes(q.toLowerCase())),[q,f]);return <div className="page"><div className="page-head"><div><div className="eyebrow">CHALLENGE BANK</div><h1>80 ways to make the idea stick.</h1><p>These are deliberately small. The hard part is explaining why your answer works.</p></div></div><div className="challenge-toolbar"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search challenges…"/><select value={f} onChange={e=>setF(e.target.value)}><option>All</option>{['Web','JavaScript','Python','Git','Database','Servers','Linux','Cloud','Security','Systems'].map(x=><option key={x}>{x}</option>)}</select></div><div className="challenge-grid">{items.map(c=><article className="challenge-card" key={c.id}><div><span className="tag">{c.track}</span><span className="difficulty">{c.difficulty}</span></div><h3>{c.title}</h3><p>{c.estimated} · {c.xp} XP</p><button className="secondary-btn">Practice</button></article>)}</div></div>}
