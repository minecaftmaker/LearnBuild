import {Link,useParams} from 'react-router-dom';import {lessonContent} from '../data/lessonContent';import {dashboardCourses} from '../data/platform';export function CoursePage(){const{id='web'}=useParams();const course=dashboardCourses.find(c=>c.id===id)||dashboardCourses[0];const lessons=(lessonContent as any)[id]||lessonContent.web;return <div className="page"><div className="course-hero"><div className="course-big-icon">{course.icon}</div><div><div className="eyebrow">{course.level} · {course.lessons} LESSONS</div><h1>{course.title}</h1><p>{course.description}</p><div className="course-hero-meta"><span>38% complete</span><span>≈ 14 hours</span><span>6 projects</span></div></div></div><div className="course-columns"><main><div className="eyebrow">COURSE ROADMAP</div><h2>Lessons</h2><div className="lesson-list">{lessons.map((l:any,i:number)=><Link to={`/lesson/${id}/${l[0]}`} key={l[0]} className="lesson-row"><span className="lesson-number">{i+1}</span><span><strong>{l[1]}</strong><small>{l[2]}</small></span><b>{i===0?'START':`${15+(i%3)*5} XP`}</b></Link>)}</div></main><aside className="course-side"><div className="side-card"><div className="eyebrow">WHAT YOU'LL BUILD</div><ul><li>Real systems, not toy syntax drills</li><li>Small tests after every concept</li><li>Projects unlock as you learn</li><li>Build logs explain your reasoning</li></ul><Link className="primary-btn" to={`/lesson/${id}/01`}>Start next lesson</Link></div></aside></div></div>}
