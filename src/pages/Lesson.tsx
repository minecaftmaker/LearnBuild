import {lessons} from '../data/lessons';import {Quiz} from '../components/Quiz';import {useState} from 'react';import {CodeEditor} from '../components/CodeEditor';
export function Lesson({id}:{id:string}){const lesson=lessons.find(l=>l.id===id)||lessons[0];const [code,setCode]=useState('// start here\n');return <article className="lesson-page"><div className="crumb">{lesson.trackId} · Stage {lesson.stage} · {lesson.minutes} min</div><h2>{lesson.title}</h2><p className="lead">{lesson.goal}</p><div className="lesson-body"><div>{lesson.steps.map((s,i)=><section className="step" key={s}><span>{i+1}</span><div><h3>{['Understand','See it','Build it','Try it'][i%4]}</h3><p>{s}</p></div></section>)}<Quiz question="Before you continue, what is the most important idea?" answers={['Repeat the definition','Understand the data flow','Copy the final code','Skip testing']} correct={1}/></div><aside><h3>Build box</h3><CodeEditor value={code} onChange={setCode}/><button className="primary full" onClick={()=>alert('In the full lab runner this executes in a sandbox.')}>Run checks</button></aside></div></article>}
