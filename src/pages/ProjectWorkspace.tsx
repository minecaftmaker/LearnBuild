import {useParams} from 'react-router-dom';import {projects} from '../data/projects';import {useState} from 'react';export function ProjectWorkspace(){const{id='http-server'}=useParams();const p=projects.find(x=>x.id===id)||projects[0];const[done,setDone]=useState<string[]>([]);return <div className="page workspace-page"><div className="workspace-head"><div><div className="eyebrow">PROJECT WORKSPACE · {p.category.toUpperCase()}</div><h1>{p.title}</h1><p>{p.difficulty} · {p.weeks} week build · {p.skills.join(' · ')}</p></div><div className="pill">{done.length}/{p.milestones.length} milestones</div></div><div className="workspace-grid"><aside className="milestones">{p.milestones.map((m,i)=><button key={m} className={done.includes(m)?'complete':''} onClick={()=>setDone(d=>d.includes(m)?d:d.concat(m))}><span>{done.includes(m)?'✓':i+1}</span>{m}</button>)}</aside><main><section className="build-brief"><div className="eyebrow">TODAY'S BUILD</div><h2>{p.milestones.find(m=>!done.includes(m))||'Project complete 🎉'}</h2><p>Do not copy a solution. First write down what you think the system must do, then implement the smallest version that can prove your idea.</p><div className="code-box"><div className="code-bar">workspace / starter</div><pre>{`// Your implementation starts here\n// 1. Write your hypothesis\n// 2. Implement the smallest experiment\n// 3. Run the test\n// 4. Explain the result`}</pre></div><div className="workspace-actions"><button className="secondary-btn">Open test output</button><button className="primary-btn" onClick={()=>{const next=p.milestones.find(m=>!done.includes(m));if(next)setDone(d=>d.concat(next))}}>Mark milestone complete</button></div></section></main></div></div>}
