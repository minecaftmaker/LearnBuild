export function Projects(){return <><h2>Project studio</h2><p className="muted">Turn lessons into things you can put in a portfolio.</p><div className="project-grid">{[['Mini Git','git'],['TinyDB','db'],['HTTP Server','server'],['Container Lab','linux'],['Hosted API','cloud'],['Toy Compiler','build']].map(([n,t])=><article className="project"><small>{t}</small><h3>{n}</h3><p>Build from a blank folder. Tests reveal what matters next.</p><button>Open studio</button></article>)}</div></>}
