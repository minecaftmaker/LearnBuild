import {tracks} from '../data/tracks';import {lessons} from '../data/lessons';import {LessonCard} from '../components/LessonCard';
export function Track({id}:{id:string}){const track=tracks.find(t=>t.id===id)||tracks[0];const list=lessons.filter(l=>track.lessonIds.includes(l.id));return <><div className="track-head"><span className="track-emoji">{track.emoji}</span><div><small>{track.estimatedMonths} month path</small><h2>{track.title}</h2><p>{track.tagline}</p></div></div><div className="lesson-list">{list.map(l=><LessonCard key={l.id} lesson={l} onOpen={()=>location.hash=`#/lesson/${l.id}`}/>)}</div></>}
