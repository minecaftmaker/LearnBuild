export interface LessonProgress{lessonId:string; completed:boolean; bestScore:number; attempts:number; lastPlayedAt?:string}
export interface CourseProgress{trackId:string; completed:number; total:number}
